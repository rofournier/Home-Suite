services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"   # accessible depuis localhost uniquement
    volumes:
      - ./data:/app/data          # persistance des bases SQLite
